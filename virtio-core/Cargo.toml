[package]
name = "virtio-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Allocation-free VirtIO transport and device protocol core for Nova OS"

[lib]
path = "src/lib.rs"

