[package]
name = "vfs-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Nova OS virtual filesystem, identity and access-control core"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
