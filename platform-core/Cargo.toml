[package]
name = "platform-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Nova OS allocation-free x86-64 platform topology and bring-up primitives"

[lib]
path = "src/lib.rs"

